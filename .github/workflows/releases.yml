name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    # Run only when necessary
    if: github.repository == 'JLAcostaEC/tabspot'
    permissions:
      contents: write # to create release (changesets/action)
      pull-requests: write # to create pull request (changesets/action)
      id-token: write # OIDC token for npm trusted publishing + provenance
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6
        with:
          # This makes Actions fetch all Git history so that Changesets can generate changelogs with the correct commits
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Ensure build leaves no tracked changes
        run: git diff --exit-code

      - name: Create Release Pull Request or Publish to npm
        id: changesets
        uses: changesets/action@v1
        # Publishing is authenticated via npm trusted publishing (OIDC) — no NPM_TOKEN.
        # Requires: a trusted publisher configured on npmjs.com for this repo + workflow,
        # `id-token: write` (above), and pnpm >= 11.3 (see packageManager in package.json).
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
        with:
          version: pnpm changeset:version
          publish: pnpm changeset:publish
